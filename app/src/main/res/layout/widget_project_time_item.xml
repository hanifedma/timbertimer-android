<?xml version="1.0" encoding="utf-8"?>
<!--
  One project's line: its colour, its name, and what it came to today.

  The name is weighted and the figure is not, so a long project name gives way
  to the number rather than pushing it off the row — the number is the reason
  anyone put this widget on their home screen.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:minHeight="34dp"
    android:paddingStart="4dp"
    android:paddingEnd="2dp">

    <ImageView
        android:id="@+id/item_dot"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_marginEnd="7dp"
        android:src="@drawable/widget_project_dot"
        android:contentDescription="@null" />

    <TextView
        android:id="@+id/item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:paddingEnd="6dp"
        android:textColor="@color/widget_text"
        android:textSize="15sp"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/item_minutes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/widget_muted"
        android:textSize="14sp"
        android:maxLines="1" />
</LinearLayout>
