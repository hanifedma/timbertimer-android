<?xml version="1.0" encoding="utf-8"?>
<!--
  The collapsed clock notification, shared by the running timer and by the
  "nothing is growing" nudge — which counts up from the last session and is
  just as much a stopwatch worth reading.

  The platform's own template draws the elapsed time in the header, at the size
  of a timestamp, because to it that is what it is. For a focus timer it is the
  whole point — the one thing worth reading from across a desk — so the content
  area is taken over to give it a size to match.

  One row, deliberately: the collapsed slot is barely two lines tall, and
  anything stacked under the clock gets clipped on the phones with the tightest
  shade. The goal bar sits below because it is a few pixels and reads as chrome.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:baselineAligned="false"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/notification_title"
            style="@style/TextAppearance.Compat.Notification.Title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1" />

        <Chronometer
            android:id="@+id/notification_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:textAppearance="@style/TextAppearance.Compat.Notification.Title"
            android:textColor="@color/timber_accent"
            android:textSize="28sp"
            android:textStyle="bold"
            android:fontFeatureSettings="tnum" />
    </LinearLayout>

    <ProgressBar
        android:id="@+id/notification_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:max="1000"
        android:progressTint="@color/timber_accent" />
</LinearLayout>
